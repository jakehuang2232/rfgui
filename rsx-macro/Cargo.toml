[package]
name = "rfgui-rsx"
version = "0.1.0-alpha.1"
edition = "2024"
description = "rsx support for rfgui"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "parsing", "extra-traits"] }
